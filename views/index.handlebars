<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Navbar</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/saved">Link</a>
      </li>
      <li class="nav-item">
          <button id="scrape-articles" class="btn btn-danger">Get New Articles!!</button>  
      </li>
    </ul>
  </div>
</nav>
<div class="jumbotron jumbotron-fluid low-opacity">
    <div class="container">
        <h1>Web Scraper</h1>
        <h3>Created By: Will Miller</h3>
    </div>
</div>
<div id="scrapedArticles" class="container my-5">
    {{#each scrapedArticles}}
        <div class="card text-light bg-dark mb-3 low-opacity">
            <div class="card-header">
                <a href="{{link}}" target="_blank" class="link-style align-left"><h4>{{title}}</h4></a>
                <button class="btn btn-danger align-right saveArticle" data-id="{{_id}}">Save</button>
            </div>
            <div class="card-body">
                <blockquote class="blockquote mb-0">
                    <p>{{teaser}}</p>
                </blockquote>
            </div>
        </div>
    {{/each}}
</div>